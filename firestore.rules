rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder — will be replaced in Phase 11
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
